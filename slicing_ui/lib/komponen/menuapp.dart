import 'package:flutter/material.dart';

class Menuapp1 extends StatelessWidget {
  const Menuapp1({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 60,
       width: 60,
        color: Colors.pink,);
  }
}